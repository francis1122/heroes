using System.Collections;
using System.Collections.Generic;
using Data;
using UnityEngine;

public class BadgeButtonController : MonoBehaviour
{

    [SerializeField] public GameObject badgePanel;
    [SerializeField] public GameObject badgeText;
    public BuildingData.BuildingCategory category = BuildingData.BuildingCategory.Building;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
